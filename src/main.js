// The Vue build version to load with the `import` command
// (runtime-only or standalone) has been set in webpack.base.conf with an alias.
// import Vue from 'vue'
// import App from './App'
import Router from './Router9'


// import store from './store'

// import aaa from './a.vue'

// new Vue({
  // el:'#app',
  // store,
  // render: xx =>xx(aaa)
  // router,
  // template: '<App/>',
  // components: { App }
// });





//引入.vue文件 一定要加后缀
// import transition from './transition.vue'

// new Vue({
 // el:'#app',
  // render:xx => xx(transition)
  // 上面这个地方的写法是1.0的我们2.0用render  代替上面的模版和组件 直接
  //把我们的
// });
